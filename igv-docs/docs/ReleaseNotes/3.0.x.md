# <p style="color: DarkRed;"> IGV 3.0 Early Access - October 2025 
<br> </p>
This section describes new features and other major updates in the _early access_ version of IGV 3.0. 
<br> It can be downloaded from [here](../Download3.0Page.md).

## IGV menu layout

The IGV menu bar has been **reorganized** and has **additional features**. Major changes include: 

1. The menu items for controlling sessions have been moved from the *File* menu to a new top-level **Sessions** menu.

2. The **Genomes** menu has a new entry for selecting the default annotations tracks for a reference genome. 
Loading hosted genomes has also changed, as described in the section on [_Reference genomes_](.#reference-genomes) below.

3. Hosted tracks previously found in _File > Load from Server_ are now found in the new top-level **Track Hubs** menu. 
The _Track Hubs_ menu also provides access to public track hubs hosted by the UCSC Genome Browser and other track hubs that can be accessed via URL.
The section below on [_Track hubs and hosted tracks_](.#track-hubs-and-hosted-tracks) describes this in more detail.

4. Loading sample information files is now done from a new top-level **Sample Info** menu; the _File_ menu can no longer be used to load sample information files.
Other items related to sample attributes have been moved from the _Tracks_ menu to the new _Sample Info_ menu.

5. The **Tracks** menu has been removed. Some items were moved to the new _Sample Info_ menu, as described above. Other items that could be accomplished in different ways have been removed from the menus:
    * _Set Track Height_: Multi-select the tracks to be changed, right-click on one of them, and select _Set Track Height_ from the pop-up menu. To multi-select tracks, use _Control_ (or _Command_ on Mac) _+ click_ on the track names to select individual tracks, or _Shift_ _+ click_ to select a range of tracks.
    * _Fit Data to Window_: Use the ![](../UserGuide/img/icon_fit_to_window.png){height=28} button in the toolbar instead.

The **new menu structure** is as follows: 

* **File**
    * Load Tracks from File
    * Load Tracks from URL
    * Save PNG Image
    * Save SVG Image
    * Exit
* **Genomes**
    * Load Genome from Host
    * Load Genome from File
    * Load Genome from URL
    * Select Default Annotations
    * Remove Genomes
* **Track Hubs**
    * [_List of categories of hosted tracks for the current reference genome_ ]
    * Select Public Track Hubs from UCSC
    * Load Track Hub from URL
    * [ _List of selected track hubs_ ]
    * [ _List of track hubs loaded by URL_ ]
* **Sample Info**
    * Load Sample Info from File 
    * Load Sample Info from URL
    * Sort Tracks by Attribute
    * Group Tracks by Attribute
    * Filter Tracks by Attribute
    * Export Track Names and Attributes
* **Sessions**
    * New Session
    * Load Session from File
    * Load Session from URL
    * Save Session
    * Reload Session
    * Autosaved Sessions 
* **View** _(has not changed in 3.0)_
    * Preferences
    * Show Name Panel
    * Show Name Panel Width
    * Show Attribute Display
    * Select Attributes to Show
    * Show Header Panel
    * Reorder Panels
    * Add new Panel
    * Go to 
* **Regions** _(has not changed in 3.0)_
    * Region Navigator
    * Gene Lists
    * Export Regions
    * Import Regions
* **Tools** _(has not changed in 3.0)_
    * Run Batch Script
    * Run igvtools
    * Find Motif
    * BLAT
    * Combine Data Tracks
* **Help** _(has not changed in 3.0)_
    * User Guide
    * Help Forum
    * About IGV

## Reference genomes 

When selecting a reference genome from the _Genomes > Load Genome from Host_ menu, you will now see a list of all genomes hosted by the UCSC Genome Browser. The list is retrieved in real time from the UCSC server, so it is always up to date. In previous releases, the list contained a much shorter curated list of genomes.

![](../UserGuide/img/genomes-hosted.png){height=440}

By default, the curated genomes from earlier IGV releases is presented at the top of the list. The list can be **filtered** by typing one or more terms in the search box at the top of the window. Or **sort** the list by clicking on any of the column headers. To **select** a genome, click on the row for that genome and then click the _Load_ button.

!!! Tip " " 
    By default, IGV will access the UCSC server on demand to load the sequence and annotations in view as you move between different loci in the genome. Alternatively, you can download the sequence and annotations to your local computer by checking the radio buttons for _Download sequence_ and _Download annotations_. This will allow IGV to function without an internet connection, but may require considerable disk space as it will download the complete sequence and annotation files.

When you click the _Load_ button, a new window will open that allows you to select the **default annotation tracks** to be loaded with the genome. The tracks are presented in categories, and you can expand or collapse a category by clicking on the + or - sign to the left of the category name. Note that some categories are presented as collapsed when you open the window. Select one or more tracks by checking the boxes by their name, and click _OK_.

![](../UserGuide/img/select_default_annotations.png){height=440}

Once loaded, the selected genome will also be added to the _genome dropdown menu_ in the IGV toolbar. Whenever you load the genome from the dropdown, the default annotation tracks you selected will be loaded automatically. To **change the default annotation tracks** for a genome, use the _Genomes > Select Default Annotations_ menu item.

## Hosted tracks and track hubs 

Hosted tracks that previously were loaded via the _File > Load from Server_ menu are now made available through the **Track Hubs** menu. The tracks are organized into categories that are listed at the top of the menu. Click on a category name to see the list of available tracks in that category. 

Below the hosted track categories are two menu items for connecting to UCSC-style **track hubs and adding them to the _Track Hubs_ menu**.

1. Click on _Select Public Track Hubs from UCSC_ to open a window that **lists all public track hubs hosted by UCSC** that are relevant for the current reference genome. The list is retrieved in real time from the UCSC server, so it is always up to date. 

    * To **add hubs** to the *Track Hubs* menu, select one or more hubs in the window by checking the boxes by their name, and click _OK_. To **remove hubs** from the menu, click on _Select Public Track Hubs from UCSC_ again, uncheck the hubs to be removed, and click _OK_. 
   
    * To **load tracks from a selected hub**, click on the hub name in the _Track Hubs_ menu. This opens a window that lists all tracks in the hub. Select one or more tracks by checking the boxes by their name, and click _OK_ to load them.

2. Click on _Load Track Hub from URL_ to **access any track hub by entering its URL**. It can be a private hub on your intranet or a public hub that is accessible by URL. This opens a window in which you can paste the URL of the hub's `hub.txt` file. Click _OK_ to open a window that allows you to select and load one or more tracks from the hub. The hub is also added to the _Track Hubs_ menu, allowing easy access to load tracks from the hub without entering the URL again.

!!! Tip " " 
    The hubs remain in the _Track Hubs_ menu across IGV sessions. The state of the menu is stored in the `hubs.txt` file in the `igv` folder in your user home folder. 

## Visual themes

A new feature in the _General_ tab of the _View > Preferences_ window allows you to select a **visual theme** for IGV. Two of the themes support a **dark mode**, which may be easier on the eyes in low-light environments. Switching to a differnt theme will require a restart of IGV before it takes effect.









    